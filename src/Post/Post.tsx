import "./Post.css"

interface PostProps {
    data: {
        image: string;
        title: string;
        description: string;
    };
}

function Post(props: PostProps) {
    return (
        <div className="wrap">
            
        </div>
    );
}

export default Post;